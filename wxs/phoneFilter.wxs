function formatPhone(phone) {
    if (!!phone) {
        phone = phone.substring(0, 3) + '****' + phone.substring(7);
    };
    return (phone)
};
module.exports = ({
    formatPhone: formatPhone,
});